import { redirect } from 'next/navigation';
import { requireUser } from '@/lib/auth';
import { prisma } from '@/lib/prisma';
export default async function NewUserPage() { const user = await requireUser(); if (!['PLATFORM_OWNER', 'ORG_ADMIN'].includes(user.role)) redirect('/app'); const orgs = user.role === 'PLATFORM_OWNER' ? await prisma.organization.findMany({ where: { isActive: true }, select: { id: true, name: true } }) : user.organization ? [{ id: user.organization.id, name: user.organization.name }] : []; return <section className="workspace narrow"><span className="eyebrow">إدارة الوصول / المستخدمون</span><h1>إضافة مستخدم</h1><p className="heading-copy">يقتصر وصول المستخدم على المنظمة المحددة فقط.</p><form className="workspace-panel form-stack" action="/api/users" method="post"><label>الاسم الكامل<input name="name" required /></label><label>البريد الإلكتروني<input name="email" type="email" dir="ltr" required /></label><label>كلمة المرور المؤقتة<input name="password" type="password" minLength={12} required /><small>12 محرفًا على الأقل.</small></label><label>المنظمة<select name="organizationId" disabled={user.role !== 'PLATFORM_OWNER'}>{orgs.map(org => <option key={org.id} value={org.id}>{org.name}</option>)}</select>{user.role !== 'PLATFORM_OWNER' && <input type="hidden" name="organizationId" value={orgs[0]?.id ?? ''} />}</label><label>الدور<select name="role"><option value="MEMBER">عضو</option><option value="STAFF">موظف</option><option value="ORG_ADMIN">مدير المنظمة</option></select></label><button className="btn btn-primary" type="submit">إنشاء المستخدم</button></form></section>; }
